{
  "name": "server0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "bundle": "bun build ./src/index.ts --outdir dist --target node --format esm",
    "start": "bun dist/index.js"
  },
  "dependencies": {
    "@vi-notes/db": "workspace:*",
    "@vi-notes/env": "workspace:*",
    "express": "^5.1.0"
  }
}
